org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Gradle 8.13 (required by AGP 8.11.1, which we use to avoid AGP 9's strict
# cross-library namespace check breaking on agora_rtc_engine's own native
# AARs) can't run under the system JDK 25 install. Pin an older JDK just for
# Gradle's own process; this doesn't affect anything else on the machine.
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.11.10-hotspot
android.useAndroidX=true
android.enableJetifier=true
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
android.bundle.enable16KPageAlignment=true
